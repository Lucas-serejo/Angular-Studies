import { Routes } from '@angular/router';
import { ApiPageComponent } from './pages/api-page/api-page.component';

export const routes: Routes = [
    { path: 'api-users', component: ApiPageComponent },
];
